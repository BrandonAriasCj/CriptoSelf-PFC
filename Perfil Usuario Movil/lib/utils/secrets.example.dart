// Plantilla. Copiá este archivo a `secrets.dart` y reemplazá los placeholders
// con los valores reales (mismos que usa Usuario Web en su `.env`).
//
// `secrets.dart` está en .gitignore — esos valores no deben quedar en el repo.

const String defaultOauthClientId = '<MOBILE_OAUTH_CLIENT_ID>';

const String defaultOauthClientSecret = '<MOBILE_OAUTH_CLIENT_SECRET>';

const String defaultGoogleServerClientId =
    '<your-web-client>.apps.googleusercontent.com';
